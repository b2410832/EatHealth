import styles from "./Home.module.scss";

const Home = () => {
    return (
        <div className={styles.container}>
            <div style={{height: '100vh', textAlign:'center'}}>首頁</div>
        </div>
    )
}

export default Home;